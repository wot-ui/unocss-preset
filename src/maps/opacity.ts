export const OPACITY_MAP: Record<string, string> = {
  disabled: 'var(--wot-opacity-disabled)',
  dimmer: 'var(--wot-opacity-dimmer)',
  overlay: 'var(--wot-opacity-overlay)',
  main: 'var(--wot-opacity-main)',
  backdrop: 'var(--wot-opacity-backdrop)',
}
